<h1>Galpões & Estoque </h1>

<% @warehouses.each do |w| %>
  <%= w.name %>
  Codigo: <%= w.code %>
  Cidade: <%= w.city %>
  <%= w.area %> m2
<% end %>

<% if @warehouses.empty? %>
  <p>Nao existem galpoes cadastrados</p>
<% end %>