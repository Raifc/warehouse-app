<style>
h1 {
text-align: center;
color: white;
background: rgb(2, 0, 36);
background: linear-gradient(90deg, rgba(2, 0, 36, 1) 0%, rgba(112, 9, 121, 1) 0%, rgba(0, 212, 255, 1) 100%);
width: 600px;
margin: 0 auto 10px;
border-radius: 25px;
}

p {
    text-align: center;
    font-style: italic;
    width: 300px;
    margin: 0 auto 10px;
    border-radius: 25px;
}
</style>

<h1>Galpões & Estoque </h1>

<% @warehouses.each do |w| %>
  <p><%= link_to(w.name, warehouse_path(w.id)) %></p>
  <p>Codigo: <%= w.code %></p>
  <p>Cidade: <%= w.city %></p>
 <p> <%= w.area %> m2 </p>
<% end %>

<% if @warehouses.empty? %>
  <p>Nao existem galpoes cadastrados</p>
<% end %>